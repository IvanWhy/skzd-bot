[project]
name = "skzd-photo-bot"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.7.0",
    "google-generativeai>=0.7.2",
    "Pillow>=10.4.0",
    "fastapi>=0.111.0",
    "uvicorn>=0.30.1",
    "aiohttp>=3.9.5"
]

[tool.vercel]
entrypoint = "bot:app"
